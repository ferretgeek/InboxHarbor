# v1.0.0 发布审计

审计日期：2026-08-09
目标：以陌生人会克隆、扫描、运行和攻击整个仓库为前提，确认公开副本不含私人原件与秘密，并达到可维护发布质量。

## 2026-08-09 展示更新

- 主页项目卡与 1280×640 社交封面改为已载入结果的批量收件工作台；README 同时保留完整工作台和入口设计。
- 画面由内置合成演示生成，账号、邮件、验证码与时间均为虚构展示数据，不连接 Microsoft 或任何真实邮箱。
- 更新后的 PNG 继续纳入尺寸、体积、元数据、OCR 与秘密扫描门禁。

## 来源与隔离

- [x] 私人原件无 Git 历史；仅发现脚本、说明文档与生成缓存。
- [x] 初始 Gitleaks 与 detect-secrets 未发现真实秘密；示例为保留示例域名与明确占位值。
- [x] 公开版在独立 `开源项目/InboxHarbor` 重建，未复制私人账号文件、缓存、绝对路径或运行输出。
- [x] 原件中的密码回退、任意主机、原始错误回显、正文终端/JSON 输出与无差别重试没有进入公开实现。

## 功能与安全

- [x] Python 与浏览器核心单元测试通过。
- [x] Ruff 与高严重度 Bandit 通过。
- [x] ASCII 路径环境中的 pip-audit 通过，运行时依赖为 0。
- [x] Gitleaks 工作树、暂存树与全部可达 Git 历史零命中；`git fsck --strict` 通过且不可达中间对象已清理。
- [x] detect-secrets 零未解决结果。
- [x] 本机未安装 Docker；GitHub CI 的独立 Docker 镜像构建作业成功。
- [x] 真实浏览器桌面与 390×844 移动端功能、四主题、TXT 解析、遮罩、最小化导出与控制台复核通过。

## 视觉与文档

- [x] 1280×720 真实界面截图已生成并确认仅含保留示例域名的合成数据。
- [x] 1280×640、实色背景、低于 1 MB 的社交预览已生成。
- [x] PNG/ICO 无 EXIF；人工视觉复核未见窗口标题、通知、私人路径、真实账号或私有地址。本机无 OCR 工具，最终再以公开视角复核。
- [x] SVG、256×256 PNG 与多尺寸 ICO favicon 已生成并由真实浏览器加载。
- [x] 中英文 README、认证、部署、安全、贡献、许可证与变更说明已补齐。

## 公开发布

- [x] Git 提交身份仅使用公开昵称与 GitHub noreply 邮箱。
- [x] GitHub 仓库、主题、双语简介、社交预览与安全设置完成；公开 `og:image` 与本地封面哈希一致。
- [x] CI、双语言 CodeQL、Docker 镜像构建与 Dependabot 初始化在线通过且无待处理更新。
- [x] 不可变 v1.0.0 Release 已发布；标签时点为 `f7ecb795dab686136782856f482d301686a45f11`。
- [x] 从未登录公开地址全新克隆复验 48 个跟踪文件、3 个 Release 时点提交、标签指向、16 项 Python 测试、3 项浏览器核心测试、依赖审计、工作树与完整历史扫描，全部通过。
- [x] 工作区根 README 与 GitHub 双语个人主页已同步；公开主页第 12 张项目卡、1280×640 图片和个人简介原文在线复核通过。

本记录在不可变 Release 之后补记闭环证据，因此主分支会比 v1.0.0 标签多一个仅修改本审计文档的提交；发布代码与标签内容没有变化。
